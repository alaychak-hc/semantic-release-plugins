{
  "name": "@alaychak-hc/semantic-release-plugins-changelog",
  "version": "0.0.002",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git@github.com:alaychak-hc/semantic-release-plugins.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prepublish": "pnpm build",
    "build": "tsc --p ./tsconfig.build.json",
    "build:pack": "pnpm build && pnpm pack && pnpm clean",
    "clean": "rimraf ./dist",
    "publish:changelog": "pnpm publish --force --no-git-checks",
    "publish:changelog:local": "pnpm publish --dry-run --force --no-git-checks"
  },
  "devDependencies": {
    "@semantic-release/error": "^4.0.0",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.9.5",
    "@types/semantic-release": "^20.0.6",
    "@types/semantic-release__error": "^3.0.3",
    "rimraf": "^5.0.5",
    "semantic-release": "^22.0.8",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@semantic-release/release-notes-generator": "^12.1.0",
    "aggregate-error": "^5.0.0",
    "fs-extra": "^11.1.1"
  }
}
